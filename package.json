{
  "name": "chatgpt-project-sidebar",
  "version": "1.0.0",
  "private": true,
  "description": "A small Manifest V3 extension that groups ChatGPT chats by project.",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "check": "node --check src/core.js && node --check src/content.js && node --check src/popup.js && npm test"
  }
}
